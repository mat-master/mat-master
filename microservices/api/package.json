{
  "name": "api",
  "packageManager": "yarn@3.2.0",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build -w --preserveWatchOutput",
    "dev-old": "yarn config set nmHoistingLimits 'workspaces' && yarn install && yarn config set nmHoistingLimits 'none' && rm -rf .aws-sam && overmind start",
    "dev": "yarn install && rm -rf .aws-sam && rm -rf ./node_modules/types && cp -r ../../common/types ./node_modules && overmind start",
    "aws-build": "sam build",
    "fix": "rm -rf node_modules && yarn config set nmHoistingLimits 'none' && yarn install"
  },
  "installConfig": {
    "hoistingLimits": "workspaces",
    "selfReferences": false
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.53.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.3",
    "stripe": "^8.205.0",
    "types": "^1.0.0",
    "validator": "^13.7.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^8.5.8",
    "@types/pg": "^8.6.4",
    "@types/validator": "^13.7.1",
    "typescript": "^4.0.5"
  }
}

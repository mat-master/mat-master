version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g yarn
            - yarn install
        build:
          commands:
            - yarn build
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: products/school-web
